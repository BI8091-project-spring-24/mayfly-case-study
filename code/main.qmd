---
title: "main document"
format: html
editor: visual
---

# Info

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

# Analysis 

## Setup

Setup: Load needed packages, and download data.

```{r prep}
## Custom install & load function
install.load.package <- function(x) {
  if (!require(x, character.only = TRUE)) {
    install.packages(x, repos = "http://cran.us.r-project.org")
  }
  require(x, character.only = TRUE)
}

package_vec <- c("here","dplyr","knitr","ggplot2") # specify package

## executing install & load for each package
sapply(package_vec, install.load.package)

```

Download Baetis rhodani occurrence data from GBIF. Download criteria:

-   Species: Baetis rhodani

-   Geographic area: Norway

-   Basis of record: Everything

-   Year range: 1950-current

```{r data download}
# Load script
source(here::here("code","download.R"))


```
